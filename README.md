# TushHub

# start backend

in dir **backend** in terminal (dev mode):

1. `npm i`
2. `npm start`

# start frontend

in dir **frontend** in terminal (dev mode):

1. `npm i`
2. `npm start`